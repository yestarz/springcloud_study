spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: dev
  redis:
    port: 6379
    host: 116.196.118.178
eureka:
  client:
    service-url:
      defaultZone : http://116.196.118.178:8761/eureka/

server:
  port: 8084

